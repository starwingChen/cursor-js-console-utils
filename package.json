{
  "name": "cursor-js-console-utils",
  "displayName": "Cursor JS Console Utils",
  "description": "Quickly insert console.log for selected variables — one-click debug logging with labeled output. Supports single/multi/empty selection. Cursor & VS Code.",
  "version": "0.1.4",
  "publisher": "starwing",
  "repository": {
    "type": "git",
    "url": "https://github.com/starwingChen/cursor-js-console-utils.git"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": ["Snippets"],
  "keywords": ["console", "utils", "debug", "javascript", "logging", "snippet", "cursor", "vscode"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cursorJsConsoleUtils.insertLogStatement",
        "title": "Insert console.log for selection"
      }
    ],
    "keybindings": [
      {
        "command": "cursorJsConsoleUtils.insertLogStatement",
        "key": "shift+alt+l",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "pkg": "npx @vscode/vsce package",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "typescript": "^5.0.0"
  }
}
